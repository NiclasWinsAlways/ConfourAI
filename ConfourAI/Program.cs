using ConfourAI.Code;

class Program
{
    static void Main(string[] args)
    {
        ConnectFourBoard board = new ConnectFourBoard();
        ConnectFourAI ai = new ConnectFourAI(board);

        while (true) // Replace with a proper game loop and conditions
        {
            // Implement game loop:
            // 1. Human player makes a move
            // 2. Check for win/lose/draw
            // 3. AI makes a move
            // 4. Check for win/lose/draw
            // Print board after each move
        }
    }
}
